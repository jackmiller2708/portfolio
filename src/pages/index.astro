---
import { Effect } from "effect";
import BaseLayout from "@layouts/BaseLayout.astro";
import EvidencePanel from "@components/organisms/EvidencePanel/EvidencePanel.astro";
import PageHero from "@components/sections/PageHero/PageHero.astro";
import CredibilityStrip from "@components/sections/CredibilityStrip/CredibilityStrip.astro";
import SectionHeader from "@components/molecules/SectionHeader/SectionHeader.astro";
import HeroSystemMap from "@islands/HeroSystemMap/HeroSystemMap.astro";
import { loadHomePageFromRepository } from "@programs/load-home-page";
import { astroContentRepository } from "@services/AstroContentRepository";
import { buildPageSeo } from "@utils/seo";

const viewModel = await Effect.runPromise(loadHomePageFromRepository(astroContentRepository));
const site = await Effect.runPromise(astroContentRepository.getSiteMeta);
const seo = buildPageSeo(site, {
  path: "/",
  description: viewModel.hero.summary,
  ogImage: "/og/home.svg"
});

const initialDiagnostic = viewModel.diagnostics[0];
---

<BaseLayout seo={seo}>
  <section class="home-hero">
    <div class="home-hero__inner u-container">
      <PageHero
        eyebrow={viewModel.hero.eyebrow}
        title={viewModel.hero.title}
        summary={viewModel.hero.summary}
        primaryCta={{ label: "Request an audit", href: "#diagnostic" }}
        secondaryCta={{ label: "See past work", href: "#past-work" }}
      />
      <p class="home-hero__support">
        My first job is to make the risk visible: where state lives, how data moves, where errors
        disappear, and which next step is worth taking.
      </p>
    </div>
  </section>

  <CredibilityStrip points={viewModel.credibilityPoints} />

  <section class="home-section home-section--diagnostic" id="diagnostic">
    <div class="u-container u-stack">
      <SectionHeader
        title="Start where the Angular system is already resisting change"
        summary="Pick the pain that sounds closest to the current risk. The path updates to a service, a reference finding, and the map state to explore next."
      />
      <div class="diagnostic" data-diagnostic-selector>
        <div class="diagnostic__options" aria-label="Frontend system pains">
          {
            viewModel.diagnostics.map((option, index) => (
              <button
                class:list={["diagnostic__option", index === 0 && "is-active"]}
                type="button"
                aria-pressed={index === 0 ? "true" : "false"}
                data-diagnostic-option
                data-id={option.id}
                data-label={option.label}
                data-pain={option.pain}
                data-service-title={option.recommendedService.title}
                data-service-href={option.recommendedService.href}
                data-audit-title={option.auditFinding.title}
                data-audit-href={option.auditFinding.href}
                data-system-state={option.systemMapState}
                data-reference-href={option.referenceHref}
                data-cta-label={option.cta.label}
                data-cta-href={option.cta.href}
              >
                <span>{option.label}</span>
                <small>{option.pain}</small>
              </button>
            ))
          }
        </div>
        {
          initialDiagnostic && (
            <article class="diagnostic__result" aria-live="polite" data-diagnostic-result>
              <p class="diagnostic__label">Recommended path</p>
              <h3 data-diagnostic-title>{initialDiagnostic.label}</h3>
              <p data-diagnostic-pain>{initialDiagnostic.pain}</p>
              <dl>
                <div>
                  <dt>Service</dt>
                  <dd>
                    <a href={initialDiagnostic.recommendedService.href} data-diagnostic-service>
                      {initialDiagnostic.recommendedService.title}
                    </a>
                  </dd>
                </div>
                <div>
                  <dt>Reference finding</dt>
                  <dd>
                    <a href={initialDiagnostic.auditFinding.href} data-diagnostic-reference>
                      {initialDiagnostic.auditFinding.title}
                    </a>
                  </dd>
                </div>
                <div>
                  <dt>Map state</dt>
                  <dd data-diagnostic-map-state>{initialDiagnostic.systemMapState}</dd>
                </div>
              </dl>
              <a class="diagnostic__cta" href={initialDiagnostic.cta.href} data-diagnostic-cta>
                {initialDiagnostic.cta.label}
              </a>
            </article>
          )
        }
      </div>
    </div>
  </section>

  <section class="home-section" id="past-work">
    <div class="u-container u-stack">
      <SectionHeader
        title="Past work without private details"
        summary="Each case study keeps the engineering shape visible while removing client-specific names, paths, and production data."
      />
      <div class="reference-card-list">
        {
          viewModel.referenceCards.map((card) => (
            <a class="reference-card" href={card.href}>
              <span>{card.title}</span>
              <dl>
                <div>
                  <dt>Problem</dt>
                  <dd>{card.problem}</dd>
                </div>
                <div>
                  <dt>Decision</dt>
                  <dd>{card.decision}</dd>
                </div>
                <div>
                  <dt>Result</dt>
                  <dd>{card.result}</dd>
                </div>
              </dl>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <section class="home-section home-section--surface" id="system-map">
    <div class="u-container u-stack">
      <SectionHeader
        title="The Angular architecture in context"
        summary="The map shows how messy frontend ownership becomes explicit once state, data access, and recovery paths have named boundaries."
      />
      <HeroSystemMap map={viewModel.heroMap} />
    </div>
  </section>

  <section class="home-section">
    <div class="u-container u-stack">
      <SectionHeader
        title="Notes from the work"
        summary="Short technical entries about decisions that are easy to miss until a system starts failing around them."
      />
      <div class="panel-grid panel-grid--compact">
        {
          viewModel.labPreview.map((post) => (
            <EvidencePanel
              eyebrow={post.topic}
              title={post.title}
              summary={post.summary}
              recommendation={post.takeaway}
              cta={{ label: "Open lab", href: `/lab/${post.slug}` }}
            />
          ))
        }
      </div>
      <a class="home-more-link" href="/lab">More in the lab</a>
    </div>
  </section>

  <section class="home-section home-section--cta">
    <div class="u-container">
      <EvidencePanel
        title={viewModel.cta.title}
        summary={viewModel.cta.summary}
        cta={{ href: viewModel.cta.href, label: viewModel.cta.label }}
      />
    </div>
  </section>
</BaseLayout>

<style>
  .home-hero {
    padding: var(--space-10) 0 var(--space-8);
  }

  .home-hero__inner {
    max-width: 980px;
  }

  .home-hero :global(.page-hero) {
    padding: 0;
  }

  .home-hero__support {
    max-width: 62ch;
    margin: var(--space-5) 0 0;
    color: var(--color-muted);
  }

  .home-section {
    scroll-margin-top: 96px;
    padding: var(--space-12) 0 var(--space-16);
    content-visibility: auto;
    contain-intrinsic-size: auto 720px;
  }

  .home-section--diagnostic {
    padding-top: var(--space-8);
  }

  .home-section--surface {
    background: var(--color-surface-2);
    border-block: var(--border-1);
  }

  .diagnostic {
    display: grid;
    gap: var(--space-6);
  }

  .diagnostic__options {
    display: grid;
    gap: var(--space-3);
  }

  .diagnostic__option {
    position: relative;
    display: grid;
    gap: var(--space-2);
    min-height: 112px;
    padding: var(--space-4);
    color: var(--color-text);
    background: var(--color-surface);
    border: var(--border-1);
    border-radius: var(--radius-2);
    cursor: pointer;
    overflow: hidden;
    text-align: left;
    transition:
      background-color var(--duration-fast) var(--ease-out-expo),
      border-color var(--duration-fast) var(--ease-out-expo),
      transform var(--duration-fast) var(--ease-spring);
  }

  .diagnostic__option::before {
    position: absolute;
    inset: 0 auto 0 0;
    width: 3px;
    background: var(--color-accent);
    content: "";
    transform: scaleY(0);
    transform-origin: bottom;
    transition: transform 220ms var(--ease-spring);
  }

  .diagnostic__option:hover {
    background: var(--color-surface-2);
    border-color: var(--color-border-accent);
  }

  .diagnostic__option.is-active {
    background: var(--color-accent-dim);
    border-color: var(--color-accent);
  }

  .diagnostic__option.is-active::before {
    transform: scaleY(1);
  }

  .diagnostic__option span {
    font-weight: 700;
  }

  .diagnostic__option small {
    color: var(--color-muted);
    font-size: var(--t-small);
  }

  .diagnostic__result,
  .reference-card {
    display: grid;
    gap: var(--space-4);
    padding: var(--space-6);
    background: var(--color-surface);
    border: var(--border-1);
    border-radius: var(--radius-2);
  }

  .diagnostic__result {
    transition:
      border-color var(--duration-state) var(--ease-standard),
      opacity 150ms var(--ease-gentle),
      transform 220ms var(--ease-out-expo);
  }

  .diagnostic__result.is-updating {
    opacity: 0;
    transform: translateY(-6px);
  }

  .diagnostic__label,
  .diagnostic__result p,
  .diagnostic__result dl,
  .diagnostic__result dd,
  .reference-card dl,
  .reference-card dd {
    margin: 0;
  }

  .diagnostic__label,
  .diagnostic__result dt,
  .reference-card dt {
    color: var(--color-accent);
    font-family: var(--font-code);
    font-size: var(--t-mono);
    font-weight: 500;
    text-transform: uppercase;
  }

  .diagnostic__result h3 {
    margin: 0;
    font-family: var(--font-body);
    font-size: var(--t-h3);
    font-weight: 700;
    line-height: 1.15;
  }

  .diagnostic__result p,
  .diagnostic__result dd,
  .reference-card dd {
    color: var(--color-muted);
  }

  .diagnostic__result dl,
  .reference-card dl {
    display: grid;
    gap: var(--space-3);
  }

  .diagnostic__result a,
  .reference-card {
    color: var(--color-text);
  }

  .diagnostic__result .diagnostic__cta {
    color: #ffffff;
  }

  .diagnostic__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: fit-content;
    min-height: 44px;
    padding: var(--space-3) var(--space-5);
    background: var(--color-accent);
    border: 1px solid var(--color-accent);
    border-radius: var(--radius-2);
    color: #ffffff;
    cursor: pointer;
    font-size: var(--t-small);
    font-weight: 700;
    text-decoration: none;
    transition:
      background-color var(--duration-fast) var(--ease-standard),
      border-color var(--duration-fast) var(--ease-standard),
      transform var(--duration-fast) var(--ease-spring);
  }

  .diagnostic__cta:hover {
    border-color: var(--color-violet);
    background: var(--color-violet);
    transform: translateY(-2px);
  }

  .panel-grid {
    display: grid;
    gap: var(--space-6);
  }

  .panel-grid--compact {
    max-width: 920px;
  }

  .reference-card-list {
    display: grid;
    gap: var(--space-4);
  }

  .reference-card {
    text-decoration: none;
    transition:
      border-color var(--duration-fast) var(--ease-spring),
      transform var(--duration-fast) var(--ease-spring);
  }

  .reference-card:hover {
    border-color: var(--color-border-accent);
    transform: translateY(-2px);
  }

  .reference-card span {
    font-family: var(--font-body);
    font-size: 1.2rem;
    font-weight: 700;
    line-height: 1.2;
  }

  .home-more-link {
    width: fit-content;
    color: var(--color-accent);
    font-weight: 700;
    text-decoration: none;
  }

  @media (min-width: 860px) {
    .diagnostic {
      grid-template-columns: minmax(0, 1.1fr) minmax(320px, 0.9fr);
      align-items: start;
    }

    .diagnostic__options {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .panel-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .diagnostic__option,
    .diagnostic__cta,
    .reference-card {
      transform: none !important;
    }
  }
</style>
